{
  "version": "1.0",
  "devDependencies": {
    "@types/finalhandler": "^1.2.3",
    "@types/node": "^20.14.2",
    "@types/serve-static": "^1.15.7",
    "esbuild": "^0.21.5",
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "finalhandler": "^1.2.0",
    "serve-static": "^1.15.0"
  },
  "scripts": {
    "build": "npx esbuild src/server.mts --bundle --platform=node --sourcemap --target=node20 --outdir=out",
    "start": "node --enable-source-maps --watch out/server.js",
    "clean": "rm -rf out/ .node_modules/"
  }
}
